module.exports = {
  plugins: {
    // https://github.com/browserslist/browserslist
    autoprefixer: {
      // 是否美化属性值 默认：true
      cascade: true,
      // 是否去掉不必要的前缀 默认：true
      remove: true
    }
  }
};
